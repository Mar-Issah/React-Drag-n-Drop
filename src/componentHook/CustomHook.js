import {useState} from 'react'

//we are building a custom hook to update the document title
//lets cr

function CustomHook() {
//     const [count, setCount]= useState(0)
    
//    const increment= ()=> {
//          setCount (prevCount => prevCount + 1)
//     }
  
//   const decrement = ()=> {
//       setCount (prevCount => prevCount - 1)
//     }

//     const reset= ()=> {
//       setCount (0)
//     }
}
export default CustomHook
