// import React from 'react'
// import ReactDOM from 'react-dom'


// //first import the ReactDOM
// //second create portal which is added to the return statement. this takes two params, the jsx to mount and the html to mount on notice the comma after the div

// //now your component is mounted on the port-root not the App

// function Portal() {
//     return ReactDOM.createPortal(
//         <div>
//             <h1>Portals demo</h1>
//         </div>,
//          document.getElementById('portal-root')
//     )
// }

// export default Portal
